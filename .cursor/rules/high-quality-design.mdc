---
description: 高质量程序设计理念与 Cursor 交互规范
globs: 
alwaysApply: true
---

# 高质量程序设计理念

## 1. 核心设计原则 (Core Design Principles)
- **单一职责原则 (SRP)**: 严禁编写“万能类”或“巨型函数”。每个模块、类、函数应当只做一件事。
- **组合优于继承**: 优先通过组合来扩展功能，避免复杂的继承链路。
- **KISS (Keep It Simple, Stupid)**: 优先选择可读性强、逻辑清晰的实现方案，而非过度设计的“奇技淫巧”。
- **DRY (Don't Repeat Yourself)**: 提取公共逻辑，但也要避免为了减少几行重复代码而引入过度抽象。

## 2. 代码健壮性要求 (Code Robustness)
- **防御式编程**: 始终验证输入参数（Null check, Range check）。
- **错误处理**: 
    - 严禁吞掉异常（Empty catch blocks）。
    - 区分“业务错误”与“系统异常”，并提供清晰的错误提示。
- **显式胜于隐式**: 变量命名必须具有描述性，严禁使用 a, b, temp 等无意义字符。

## 3. 类型安全与注释 (Typing & Documentation)
- **严格类型检查**: 如果使用 TypeScript/Python (Typing)/Go，严禁使用 any 或弱类型。所有接口定义必须明确。
- **自解释代码**: 代码本身应具备可读性。注释应解释“为什么这样做”而不是“正在做什么”。
- **JSDoc/Docstring**: 公共函数必须包含标准的文档注释，说明参数、返回值及潜在副作用。

## 4. 性能与现代实践 (Performance & Modern Practice)
- **异步优先**: 非阻塞 I/O 必须使用 async/await，并合理处理并发。
- **内存意识**: 及时释放资源（如：文件流、数据库连接、监听器）。
- **不可变性 (Immutability)**: 优先使用 const 或只读数据结构，减少副作用，使状态变化可追溯。

## 5. Cursor 交互指令 (Operational Instructions)
- **先思考再编码**: 在生成大段代码前，请先用中文/英文简述你的设计思路和模块拆解。
- **增量修改**: 除非是创建新文件，否则优先对现有代码进行重构和增量修改，不要随意删除已有的关键逻辑。
- **代码 Review 模式**: 每次写完代码后，请自我检查是否符合上述设计原则，并在最后给出简短的重构建议。
